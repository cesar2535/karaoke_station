@css {
  @import "suitcss-utils-display";
}

$Footer-bgColor = #000000;

.Footer {
  display flex
  justify-content center
  // background-image linear-gradient( alpha($Footer-bgColor, 0.5), alpha($Footer-bgColor, 0.5) )
  background-color $Footer-bgColor
  width 100%
  height 70px
  position fixed
  bottom 0
}
