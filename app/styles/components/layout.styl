#root {

}

.App {
  // display flex
  // flex-direction column
  width 100vw
  height 100vh
  overflow hidden
}

.Queue, .Favorites, .History, .Songbook {
  position relative
}

.Queue--playlist {
  width 620px
  margin auto
}

.Queue-head {
  display flex
  background-color alpha(#000, 0.7)
  padding 10px 30px

  & > * {
    flex 1
  }
}
